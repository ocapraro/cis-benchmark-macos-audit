# Ensure /home is a separate partition
# 1. Backup data
# 2. Create a new partition for /home
# 3. Format the new partition
# 4. Mount the new partition to /home
# 5. Restore data
# 6. Update /etc/fstab to mount /home on boot
exit 2
