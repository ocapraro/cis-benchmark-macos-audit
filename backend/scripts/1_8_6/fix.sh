# To disable Xwayland, edit /etc/gdm/custom.conf and set WaylandEnable=false
# 1. Open /etc/gdm/custom.conf in a text editor.
# 2. Locate the [daemon] section.
# 3. Add or modify the line to read: WaylandEnable=false
# 4. Save the file and restart the gdm service or reboot the system.
